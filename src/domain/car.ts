export class Car {
  brand: string;
  model: string;
  vin?: string;
  mileage?: number;
  releaseDate?: Date;
  color?: string; //todo enum

  bodyType?: string; //todo: enum
  engineType?: string; //todo: enum
}
